$('#releases').html("<%=raw escape_javascript(render(partial: 'releases', object: @releases)) %>");
$('#releases_pagination').html("<%=raw escape_javascript(render(partial: 'releases_pagination', object: @albums)) %>");
